const quarterValue = (num1) => {
     const quarter = num1 / 4
    return quarter
}

// Invoke the function and store the return value
const newNum = quarterValue(1000)

// Log the return value
console.log(newNum)